---
import Card from './Card.astro';
 
const { story } = Astro.props;
---
 
{story?.content?.body?.map((blok: { component: any; }) => {
  switch(blok.component) {
    case 'task':
      return <Card blok={blok} />
  }
})}