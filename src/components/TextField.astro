---
import Label from "./Label.astro";

const { label, id, placeholder } = Astro.props;
---

<Label id={id}>
  {label}
</Label>
<input class='border-grey-300 border-opacity-25 border border-solid rounded body-l py-2 px-4 invalid:border-red-200' id={id} type="text" placeholder={placeholder}>